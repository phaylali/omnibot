{
  "name": "omnibot",
  "version": "1.0.0",
  "description": "A Discord bot built with Bun and TypeScript",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "deploy": "bun run src/deploy.ts",
    "dev": "bun --watch run src/index.ts"
  },
  "dependencies": {
    "discord.js": "^14.16.0"
  }
}
